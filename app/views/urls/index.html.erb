<p style="color: green"><%= notice %></p>

<% content_for :title, "Urls" %>

<h1>View shortened Urls</h1>

<div id="urls">
  <% @urls.each do |url| %>
    <%= render url %>
    <p>
      <%= link_to "Show this url", url %>
    </p>
  <% end %>
</div>
